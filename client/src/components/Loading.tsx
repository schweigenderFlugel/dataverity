/**
 * Componente de carga de pantalla
 * @description Este componente se muestra mientras se carga la aplicación.
 * @returns {JSX.Element}
 */
const Loading = () => {
  return <div>Cargando...</div>;
};

export default Loading;
