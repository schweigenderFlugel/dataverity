apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
  labels:
    app: {{ .Values.name }}
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.name }}
  ports:
    - port: 5173
      protocol: TCP
      targetPort: 80
