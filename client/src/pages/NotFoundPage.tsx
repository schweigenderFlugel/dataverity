/**
 * Página de error 404
 * @description Esta página se muestra cuando el usuario intenta acceder a una ruta que no existe.
 * @returns {JSX.Element}
 */
const NotFoundPage = () => {
  return <div>404 Not Found</div>;
};

export default NotFoundPage;
