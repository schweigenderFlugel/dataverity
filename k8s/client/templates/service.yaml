apiVersion: v1
kind: Service
metadata:
  name: client
  labels:
    app: client
spec:
  type: LoadBalancer
  selector:
    app: client
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
